# AgentLens Backend

## Local Development

1. `cd backend`
2. `pip install -e ".[dev]"`
3. `uvicorn app.main:app --reload`

The API will start on `http://127.0.0.1:8000`, with Swagger UI at `http://127.0.0.1:8000/docs`.

